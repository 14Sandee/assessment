module.exports = {
    content: [
        './*.html', // Matches any HTML files in the root directory
        './**/*.html' // Matches any HTML files in subdirectories
      ],
    theme: {
      extend: {},
    },
    plugins: [],
  }